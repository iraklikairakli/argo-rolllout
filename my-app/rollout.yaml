apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: pataradzma
  namespace: default
spec:
  replicas: 3
  strategy:
    canary:
      steps:
        - setWeight: 30
        - pause: {}
  selector:
    matchLabels:
      app:
  template:
    metadata:
      labels:
        app: pataradzma
    spec:
      containers:
        - name: pataradzma
          image: nginx:latest
          ports:
            - containerPort: 80
